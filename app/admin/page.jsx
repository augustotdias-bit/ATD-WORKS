'use client';
import {useState} from 'react';
export default function Admin(){const [file,setFile]=useState(null);const [form,setForm]=useState({title:'',medium:'',dimensions:'',date:'',availability:'Available'});return <main className="site"><header className="header"><a className="brand" href="/">ATD WORKS</a><a className="nav" href="/">Back to archive</a></header><section className="hero"><h1>ADD WORK</h1><p>Artwork entry interface. Upload an image and record the catalogue information.</p><form style={{maxWidth:620,display:'grid',gap:18}} onSubmit={e=>{e.preventDefault();alert('Entry prepared. Persistent storage will be connected next.')}}><input required placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input required placeholder="Medium" value={form.medium} onChange={e=>setForm({...form,medium:e.target.value})}/><input required placeholder="Dimensions" value={form.dimensions} onChange={e=>setForm({...form,dimensions:e.target.value})}/><input required type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/><select value={form.availability} onChange={e=>setForm({...form,availability:e.target.value})}><option>Available</option><option>Sold</option><option>Not for sale</option></select><input type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><button type="submit">{file?'ADD WORK':'SAVE ENTRY'}</button></form></section></main>}